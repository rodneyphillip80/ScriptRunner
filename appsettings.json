{
  "Connection": {
    // Update these values to point at your SQL Server instance. These
    // settings should be overridden via environment variables or secrets
    // when deploying to production.
    "Server": "tcp:your-sql-server.database.windows.net,1433",
    "Database": "YourDatabase",
    "User": "youruser",
    "Password": "yourpassword"
  },
  // Directory where SQL migration scripts are stored. The MigrationRunner
  // scans this folder at startup and executes scripts in lexical order.
  "ScriptsDirectory": "./Scripts"
}